import React from 'react';
import { ContentHeader } from '@/components/common';

function Show({ title }) {
  return (
    <div>
      <ContentHeader title={title} />
    </div>
  );
}

export default Show;
