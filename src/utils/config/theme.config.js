const themeConfig = {
  theme: {
    darkMode: false,
    semiDarkMode: false,
    skin: 'default',
    contentWidth: 'full',
    menu: {
      isCollapsed: false,
      isHidden: false,
    },
  },
};

export default themeConfig;
